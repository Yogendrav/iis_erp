#page-top
  #top-search
    %table{border: "0", cellpadding: "0", cellspacing: "0"}
      %tbody
        %tr
          %td
            .whitebig
              - if user_signed_in?
                Welcome :-
                = current_user.emp_name